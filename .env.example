# Waitlist confirmation emails (Resend — https://resend.com )
RESEND_API_KEY=re_xxxxxxxx
# Must match a verified domain in Resend, e.g. "First Sons <hello@yourdomain.com>"
# For quick tests only, Resend may provide a sandbox sender — check their docs.
WAITLIST_FROM_EMAIL="First Sons <onboarding@yourdomain.com>"
